@model PlantCareAI.Models.FertilizingRecord

@{
    ViewData["Title"] = "Edit Fertilizer Record";
}

<div class="container page-header">
    <span class="section-eyebrow">Update care record</span>

    <h1 class="fw-bold mt-1">
        🌱 Edit Fertilizer Record
    </h1>

    <p class="text-muted mb-0">
        Update fertilizing information for
        <strong>@ViewBag.PlantName</strong>.
    </p>
</div>

<div class="container pb-5">

    <div class="row justify-content-center">

        <div class="col-md-7">

            <div class="form-card">

                <div class="form-card-accent"></div>

                <div class="card-body p-4 p-md-5">

                    <form asp-action="EditFertilizer"
                          method="post">

                        <input type="hidden"
                               asp-for="Id" />

                        <input type="hidden"
                               asp-for="PlantId" />

                        <div asp-validation-summary="ModelOnly"
                             class="text-danger mb-3">
                        </div>


                        <!-- Date & Time -->
                        <div class="mb-3">

                            <label asp-for="FertilizedAt"
                                   class="form-label">
                                Date &amp; Time
                            </label>

                            <input asp-for="FertilizedAt"
                                   type="datetime-local"
                                   asp-format="{0:yyyy-MM-ddTHH:mm}"
                                   class="form-control" />

                            <span asp-validation-for="FertilizedAt"
                                  class="text-danger">
                            </span>

                        </div>


                        <!-- Fertilizer -->
                        <div class="mb-3">

                            <label asp-for="Fertilizer"
                                   class="form-label">
                                Fertilizer
                            </label>

                            <input asp-for="Fertilizer"
                                   class="form-control"
                                   placeholder="e.g. Organic fertilizer" />

                            <span asp-validation-for="Fertilizer"
                                  class="text-danger">
                            </span>

                        </div>


                        <!-- Notes -->
                        <div class="mb-4">

                            <label asp-for="Notes"
                                   class="form-label">
                                Notes
                            </label>

                            <textarea asp-for="Notes"
                                      class="form-control"
                                      rows="4"
                                      placeholder="Add any notes about fertilizing..."></textarea>

                            <span asp-validation-for="Notes"
                                  class="text-danger">
                            </span>

                        </div>


                        <!-- Buttons -->
                        <div class="d-flex gap-2">

                            <button type="submit"
                                    class="btn btn-success">
                                💾 Save Changes
                            </button>

                            <a asp-controller="Plants"
                               asp-action="Details"
                               asp-route-id="@Model.PlantId"
                               class="btn btn-outline-secondary">
                                Cancel
                            </a>

                        </div>

                    </form>

                </div>

            </div>

        </div>

    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync(
            "_ValidationScriptsPartial");
    }
}